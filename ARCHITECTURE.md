# CV 算法可视化平台 — 总体架构设计

## 一、项目愿景

构建一个 **「计算机视觉算法可视化通识教育平台」**，核心理念是**可理解性优先**。让即使没有数学背景的业余爱好者，也能通过视觉化的方式，直观理解每个算法从输入 → 处理过程 → 输出的完整流程。

**目标用户**：对计算机视觉完全不懂的人。操作本系统之后，能对 CV 有一定了解。

### 核心设计原则

| 原则 | 说明 |
|---|---|
| **零黑盒** | 每一个中间张量/矩阵都可以作为图像或热力图查看 |
| **参数游乐场** | 每个参数都是一个滑块；调整后即时重新渲染 |
| **叙事流** | 算法以故事形式呈现：「这是输入 → 我们先做什么 → 为什么 → 这是结果」 |
| **直觉优先** | 先展示效果建立直觉，数学细节默认折叠，按需展开 |
| **纯手写算法** | 所有 CV 算法用纯 NumPy 实现，不调用 OpenCV 等高层 API；每一行计算都可追踪 |
| **动静分离** | 高频刷新的像素级渲染（热力图、特征图）用 Canvas；低频交互的矢量元素（连线、坐标系）用 SVG |

---

## 二、整体架构

```
┌──────────────────────────────────────────────────────────────┐
│                      前端 (Browser)                           │
│                                                               │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐ │
│  │ 阶段画廊 │  │ 算法详情 │  │ 难度阶梯 │  │   全局搜索   │ │
│  │ (Gallery)│  │ (Detail) │  │ (Ladder) │  │   (Search)   │ │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └──────┬───────┘ │
│       └──────────────┴─────────────┴───────────────┘         │
│                          │                                     │
│                    ┌─────┴─────┐                              │
│                    │ 渲染引擎   │                              │
│                    │ Canvas 2D  │  ← 热力图、梯度图、特征图      │
│                    │ + SVG      │  ← 连线、高亮框、方向箭头      │
│                    └─────┬─────┘                              │
│                          │                                     │
│              ┌───────────┴───────────┐                        │
│              │                       │                        │
│        ┌─────┴─────┐          ┌─────┴─────┐                  │
│        │ 静态页面   │          │ API 客户端 │                  │
│        │ (iframe)  │          │  (fetch)   │                  │
│        └───────────┘          └─────┬─────┘                  │
└─────────────────────────────────────┼────────────────────────┘
                                      │ HTTP/JSON
┌─────────────────────────────────────┼────────────────────────┐
│                      后端 (Python)                            │
│                                     │                         │
│                          ┌─────────┴─────────┐               │
│                          │  Flask + CORS      │               │
│                          └─────────┬─────────┘               │
│                                    │                          │
│              ┌─────────────────────┼─────────────────────┐   │
│              │                     │                     │   │
│        ┌─────┴─────┐        ┌─────┴─────┐        ┌─────┴──┐│
│        │  NumPy    │        │ imageio   │        │ Pillow  ││
│        │ (核心计算)│        │ (图像IO)  │        │(base64) ││
│        └───────────┘        └───────────┘        └────────┘│
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │              模块注册系统 (Module Registry)               │ │
│  │  importlib 自动扫描 phase1~5 目录 → AlgorithmModule 子类 │ │
│  │  自动注册 → /api/modules 返回阶段式导航数据               │ │
│  └─────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

### 技术栈纯洁性约束

| 层面 | 允许 | 禁止 |
|---|---|---|
| **前端** | HTML5, CSS3, Vanilla JS, Canvas 2D, SVG | React, Vue, ECharts, Chart.js, 任何外部 UI/图表库 |
| **后端** | Python 3.x, Flask, NumPy, imageio, Pillow | OpenCV, scikit-image, PyTorch, 任何高层 CV 库 |
| **算法** | 纯 NumPy 手写矩阵运算 | `cv2.Canny()`, `cv2.SIFT_create()` 等黑盒调用 |

---

## 三、信息架构（三层模型 + 五阶段叙事）

### 第一层：阶段画廊（顶层导航）

按**学习路径的阶段**组织，而非按数学类别。用户从左到右、从上到下就是一条完整的学习曲线：

```
┌──────────────────────────────────────────────────────────┐
│                 CV 通识教育 — 学习路径                      │
│                                                            │
│  ┌──────────────────┐  ┌──────────────────┐               │
│  │  阶段一: 基础原语  │  │  阶段二: 经典CV   │               │
│  │  ▸ 灰度转换       │  │  ▸ 边缘检测      │               │
│  │  ▸ 基础卷积       │  │  ▸ Harris 角点   │               │
│  │  ▸ 实时滤镜       │  │  ▸ SIFT 特征     │               │
│  └──────────────────┘  └──────────────────┘               │
│                                                            │
│  ┌──────────────────┐  ┌──────────────────┐               │
│  │  阶段三: 中级视觉  │  │  阶段四: 深度学习  │               │
│  │  ▸ 特征匹配+拼接  │  │  ▸ LeNet-5       │               │
│  │  ▸ 频域分析       │  │  ▸ 目标检测 [待]  │               │
│  │                   │  │  ▸ 语义分割 [待]  │               │
│  │                   │  │  ▸ 实例分割 [待]  │               │
│  └──────────────────┘  └──────────────────┘               │
│                                                            │
│  ┌──────────────────┐                                     │
│  │  阶段五: 前沿论文  │  ← ViT, DETR, SAM, NeRF, ...       │
│  │  (预留扩展)       │                                     │
│  └──────────────────┘                                     │
└──────────────────────────────────────────────────────────┘
```

每个阶段卡片带有：阶段编号、名称、模块芯片（点击直接进入）、数量统计。

### 第二层：算法详情页（iframe 嵌入）

单个算法的完整叙事流程。每个算法是一个独立的 HTML 页面，在主外壳的 iframe 中加载：

```
┌─────────────────────────────────────────────────────────┐
│  📌 Canny 边缘检测                         难度: ★★☆     │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 💡 直觉                                          │   │
│  │ "想象你在看一张照片，你想找出所有物体的轮廓。      │   │
│  │  Canny 算法就像是先用毛玻璃模糊一下画面去掉噪点，  │   │
│  │  然后找出亮度变化最剧烈的地方..."                 │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 📥 输入面板: [上传图片] [选择示例图]              │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 🔬 流水线视图 (Pipeline Strip)                   │   │
│  │                                                  │   │
│  │ [原图] → [灰度化] → [高斯模糊] → [梯度计算] →    │   │
│  │ [非极大值抑制] → [双阈值] → [边缘跟踪] → [结果]  │   │
│  │                                                  │   │
│  │ 每个阶段: 缩略图 + 一句话解释 + 可展开查看中间结果 │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 📊 输出: [原图] ← 拖拽滑块 → [结果]              │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 📐 深入数学 (默认折叠): 梯度公式、双阈值原理...   │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 第三层：交互式游乐场（规划中）

用户可以像搭积木一样将算法阶段连接起来，构建自定义处理流水线。

---

## 四、核心可视化模式：「流水线条带」

这是平台最重要的 UI 组件。每个算法渲染为一条**水平流水线条带**，展示数据流经每个处理阶段的形态：

```
┌──────┐     ┌──────────┐     ┌──────────┐     ┌──────┐
│ 输入  │ ──→ │ 阶段 1   │ ──→ │ 阶段 2   │ ──→ │ 输出  │
│      │     │          │     │          │     │      │
│ [🐱] │     │ 去噪声   │     │ 找边缘   │     │ [📋] │
│      │     │ "我们平滑 │     │ "计算每个 │     │      │
│      │     │ 图像来去  │     │ 像素的变  │     │      │
│      │     │ 除噪点"   │     │ 化率"     │     │      │
└──────┘     └──────────┘     └──────────┘     └──────┘
  蓝色边框      灰色边框         灰色边框        绿色边框
```

### 每个阶段卡片包含：
- **可视化缩略图**：该阶段的图像/热力图/直方图
- **一句话解释**：用日常语言说明这一步在做什么
- **悬停放大**：鼠标悬停显示大图 + 与上一阶段的对比
- **「如果跳过这步会怎样？」按钮**：点击查看退化结果，直观理解该步骤存在的意义

### 关键 UX：「差异视图」
任意两个相邻阶段之间，提供**差异高亮视图**——用热力色标出哪些像素发生了变化。这是最强的可理解性特性，回答了「这一步到底干了什么？」

---

## 五、对比模式（核心学习特性）

同一任务的不同算法并列对比，同一输入，多维评判：

```
┌─────────────────────────────────────────────────────────┐
│  边缘检测算法对比                 输入: [上传的图片]     │
│                                                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │
│  │   Canny     │  │   Sobel     │  │  Laplacian  │     │
│  │             │  │             │  │             │     │
│  │  [结果图]   │  │  [结果图]   │  │  [结果图]   │     │
│  │             │  │             │  │             │     │
│  │ ✅ 清晰细边 │  │ ✅ 简单快速 │  │ ✅ 检测全面 │     │
│  │ ❌ 需调参数 │  │ ❌ 边缘较粗 │  │ ❌ 对噪声   │     │
│  │             │  │ ❌ 噪声较多 │  │    敏感     │     │
│  └─────────────┘  └─────────────┘  └─────────────┘     │
└─────────────────────────────────────────────────────────┘
```

这教会用户**算法选择的直觉**——区分「记住步骤」和「真正理解」的关键。

---

## 六、算法覆盖范围与分阶段计划

### 阶段一：基础原语（先行建设，后续算法全都依赖）

| 算法 | 说明 | 计算位置 |
|---|---|---|
| **卷积操作** | 滑动窗口可视化，所有后续算法的基础 | 浏览器 |
| **高斯模糊** | 平滑去噪 | 浏览器 |
| **Sobel 梯度** | 一阶导数，边缘检测基础 | 浏览器 |
| **色彩空间转换** | RGB ↔ HSV ↔ Lab | 浏览器 |
| **直方图** | 计算与均衡化 | 浏览器 |
| **阈值化** | 全局 / 自适应 / Otsu | 浏览器 |

### 阶段二：经典 CV 核心算法

| 算法 | 说明 | 计算位置 |
|---|---|---|
| **Canny 边缘检测** | 多阶段流水线，非常适合展示 | 浏览器 |
| **Harris 角点检测** | 角点响应可视化 | 浏览器 |
| **SIFT 特征** | 尺度空间 + 关键点描述 | 浏览器 |
| **Hough 变换** | 直线与圆形检测，参数空间可视化 | 浏览器 |
| **形态学操作** | 腐蚀 / 膨胀 / 开 / 闭 | 浏览器 |
| **轮廓查找** | 轮廓层级与分析 | 浏览器 |

### 阶段三：中级视觉

| 算法 | 说明 | 计算位置 |
|---|---|---|
| **分水岭分割** | 地形浸没模拟 | 浏览器/服务端 |
| **GrabCut** | 交互式前景提取 | 服务端 |
| **SLIC 超像素** | 聚类可视化 | 浏览器 |
| **HOG + SVM** | 经典目标检测流程 | 服务端 |
| **光流** | Lucas-Kanade / Farneback | 浏览器 |
| **立体匹配** | 视差图与深度重建 | 服务端 |
| **图像拼接** | 特征匹配 + 单应性 + 融合 | 服务端 |

### 阶段四：深度学习时代

| 算法 | 说明 | 计算位置 |
|---|---|---|
| **CNN 基础** | 卷积层可视化，滤波器激活图 | 服务端 |
| **图像分类** | ResNet + Grad-CAM 热力图 | 服务端 |
| **YOLO** | 检测流水线可视化 | 服务端 |
| **U-Net** | 分割跳跃连接可视化 | 服务端 |
| **GAN** | 生成器/判别器博弈过程 | 服务端 |
| **扩散模型** | 去噪步骤逐步可视化 | 服务端 |

### 阶段五：前沿论文算法（2020-2025）

#### 5.1 视觉 Transformer 家族

| 算法 | 论文 | 说明 | 计算位置 |
|---|---|---|---|
| **ViT** | *An Image is Worth 16x16 Words* (Dosovitskiy et al., ICLR 2021) | 将图像切成 Patch，像 NLP 的 Token 一样送入 Transformer。可视化 Patch 划分、位置编码、自注意力图 | 服务端 |
| **Swin Transformer** | *Swin Transformer* (Liu et al., ICCV 2021) | 分层 Transformer，引入窗口注意力和移位窗口。可视化窗口划分、层级特征图 | 服务端 |
| **DINO / DINOv2** | *Emerging Properties in Self-Supervised Vision Transformers* (Caron et al., ICCV 2021 / 2023) | 自监督 ViT，注意力图自动形成语义分割。可视化无标签训练过程、注意力涌现的语义区域 | 服务端 |
| **MAE** | *Masked Autoencoders* (He et al., CVPR 2022) | 随机遮盖 75% 图像块，让模型重建。可视化遮盖→编码→解码→重建的完整流程 | 服务端 |

#### 5.2 检测新范式

| 算法 | 论文 | 说明 | 计算位置 |
|---|---|---|---|
| **DETR** | *End-to-End Object Detection with Transformers* (Carion et al., ECCV 2020) | 用 Transformer 直接输出检测框，去掉 NMS/Anchor。可视化 Object Query 的注意力演化、二分图匹配 | 服务端 |
| **DINO** | *DINO: DETR with Improved DeNoising Anchor Boxes* (Zhang et al., ICLR 2023) | DETR 改进版，收敛更快。可视化对比去噪训练与标准训练的差异 | 服务端 |
| **Grounding DINO** | *Grounding DINO* (Liu et al., ECCV 2024) | 开放词汇检测，输入文字描述即可检测任意物体。可视化文本-图像特征融合过程 | 服务端 |

#### 5.3 分割大一统

| 算法 | 论文 | 说明 | 计算位置 |
|---|---|---|---|
| **SAM** | *Segment Anything* (Kirillov et al., ICCV 2023) | Meta 的分割基础模型。可视化 Prompt Encoder（点/框/掩码）→ Image Encoder → Mask Decoder 的全流程 | 服务端 |
| **SAM 2** | *SAM 2: Segment Anything in Images and Videos* (Ravi et al., 2025) | 扩展到视频。可视化记忆注意力机制、时间传播过程 | 服务端 |
| **Mask2Former** | *Masked-attention Mask Transformer* (Cheng et al., CVPR 2022) | 统一分割架构（语义/实例/全景）。可视化掩码注意力与多尺度特征融合 | 服务端 |

#### 5.4 多模态·视觉-语言

| 算法 | 论文 | 说明 | 计算位置 |
|---|---|---|---|
| **CLIP** | *Learning Transferable Visual Models* (Radford et al., ICML 2021) | 4亿图文对训练的对比学习模型。可视化文本-图像双塔架构、对比损失、零样本分类流程 | 服务端 |
| **BLIP-2** | *BLIP-2* (Li et al., ICML 2023) | 用 Q-Former 桥接视觉编码器和 LLM。可视化 Q-Former 的查询-键交互机制 | 服务端 |

#### 5.5 生成式模型前沿

| 算法 | 论文 | 说明 | 计算位置 |
|---|---|---|---|
| **DDPM** | *Denoising Diffusion Probabilistic Models* (Ho et al., NeurIPS 2020) | 扩散模型奠基之作。可视化加噪过程→去噪过程，每一步的噪声预测 | 服务端 |
| **Stable Diffusion (LDM)** | *High-Resolution Image Synthesis with Latent Diffusion Models* (Rombach et al., CVPR 2022) | 在潜空间做扩散，大幅降低计算量。可视化 VAE 编解码 + UNet 去噪 + Cross-Attention 文本控制 | 服务端 |
| **ControlNet** | *Adding Conditional Control to Text-to-Image Diffusion Models* (Zhang et al., ICCV 2023) | 为扩散模型增加空间控制信号（边缘/深度/姿态）。可视化控制信号如何引导生成过程 | 服务端 |
| **StyleGAN** | *A Style-Based Generator Architecture* (Karras et al., CVPR 2019/2020/2021) | 风格可控的人脸生成。可视化风格混合、隐空间插值、解耦表征 | 服务端 |
| **DiT** | *Scalable Diffusion Models with Transformers* (Peebles & Xie, ICCV 2023) | 用 Transformer 替代 UNet 做扩散。可视化 Patch 化潜空间 + Transformer 去噪 | 服务端 |
| **Flux** | (Black Forest Labs, 2024) | DiT + Flow Matching + 多模态引导。可视化 Flow Matching 与 DDPM 的路径差异 | 服务端 |

#### 5.6 3D 视觉与神经渲染

| 算法 | 论文 | 说明 | 计算位置 |
|---|---|---|---|
| **NeRF** | *NeRF: Representing Scenes as Neural Radiance Fields* (Mildenhall et al., ECCV 2020) | 用 MLP 表示 3D 场景，体渲染生成新视角。可视化采样射线→位置编码→MLP→体渲染的完整管线 | 服务端 |
| **3D Gaussian Splatting** | *3D Gaussian Splatting* (Kerbl et al., SIGGRAPH 2023) | 用3D高斯球替代 MLP，实时渲染。可视化 SfM 初始化→高斯致密化→可微光栅化 | 服务端 |
| **DUSt3R** | *DUSt3R* (Wang et al., CVPR 2024) | 无需相机参数，从任意图像对直接预测 3D 点云。可视化 Transformer 如何联合推断几何与位姿 | 服务端 |

#### 5.7 自监督 / 对比学习

| 算法 | 论文 | 说明 | 计算位置 |
|---|---|---|---|
| **SimCLR** | *A Simple Framework for Contrastive Learning* (Chen et al., ICML 2020) | 对比学习经典框架。可视化数据增强→正负样本对→对比损失的直观过程 | 服务端 |
| **MoCo** | *Momentum Contrast* (He et al., CVPR 2020) | 动量队列实现大规模对比学习。可视化动态字典、动量编码器更新的机制 | 服务端 |
| **BYOL** | *Bootstrap Your Own Latent* (Grill et al., NeurIPS 2020) | 无需负样本的自监督学习。可视化 Online/Target 双网络互相预测的过程 | 服务端 |
| **I-JEPA** | *Image Joint Embedding Predictive Architecture* (Assran et al., CVPR 2023) | 在表征空间而非像素空间做预测。可视化上下文块→目标块表征预测的抽象过程 | 服务端 |

#### 5.8 跟踪与视频理解

| 算法 | 论文 | 说明 | 计算位置 |
|---|---|---|---|
| **ByteTrack** | *ByteTrack* (Zhang et al., ECCV 2022) | 利用低分检测框做多目标跟踪。可视化高分/低分框的关联策略和数据流 | 服务端 |
| **BotSORT** | *BoT-SORT* (Aharon et al., 2022) | 融合 ReID 特征 + 相机运动补偿的跟踪器。可视化外观特征匹配与运动预测的融合 | 服务端 |

#### 5.9 姿态估计

| 算法 | 论文 | 说明 | 计算位置 |
|---|---|---|---|
| **OpenPose** | *OpenPose* (Cao et al., TPAMI 2019) | 实时多人 2D 姿态估计。可视化 PAF（部分亲和场）+ 关键点热力图 → 二分图匹配 | 浏览器/服务端 |
| **MediaPipe Pose** | (Google, 2020) | 轻量级姿态估计。可视化轻量检测+跟踪的两阶段流水线 | 浏览器 |
| **ViTPose** | *ViTPose* (Xu et al., NeurIPS 2022) | 用纯 ViT 做姿态估计。可视化 Transformer 如何直接输出关键点热力图 | 服务端 |

> **注**：阶段五的算法全部需要服务端 GPU 计算，且每个算法的预计算缓存策略尤为重要——对精选示例图像预先跑完所有中间状态，用户打开页面即可看到完整的流水线可视化，无需等待。

### 前沿算法的特殊可视化设计

前沿论文算法与经典 CV 算法有本质不同，需要专门的可视化策略：

#### 5.A Transformer 架构可视化

```
┌─────────────────────────────────────────────────────────────┐
│  ViT 前向传播可视化                                         │
│                                                              │
│  [输入图像] → [Patch切分] → [线性投影+位置编码] →            │
│  [Transformer Encoder ×12] → [CLS Token] → [分类头] → 结果  │
│                                                              │
│  关键可视化：                                                │
│  ┌────────────┐  ┌────────────┐  ┌────────────────────────┐ │
│  │ Patch划分  │  │ 注意力图   │  │ 各层CLS关注的图像区域  │ │
│  │ 16×16网格  │  │ 热力图叠加 │  │ 逐层演化动画          │ │
│  └────────────┘  └────────────┘  └────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

#### 5.B 注意力图可视化（Transformer 类算法的核心）

这是理解 ViT/DETR/DINO/SAM 的关键：
- **自注意力图**：展示 `[CLS]` Token 关注图像的哪些区域
- **交叉注意力图**（DETR）：展示 Object Query 关注图像的哪些位置
- **多头注意力对比**：并排展示多个注意力头各自关注的区域，理解「不同头学不同模式」
- **逐层演化**：用动画展示注意力从浅层（边缘/纹理）到深层（语义/物体）的变化

#### 5.C 潜空间可视化（生成模型的核心）

```
Stable Diffusion 潜空间之旅：

[输入文本] → [CLIP Text Encoder] → [文本嵌入]
                                          ↓
[输入图像] → [VAE Encoder] → [潜空间 z₀] → [UNet 去噪循环] → [VAE Decoder] → [生成图像]
                                  ↑              ↑
                            [随机噪声]    [Cross-Attention:
                                          文本控制信号]

每个去噪步骤展示：
┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐
│ z₁₀₀₀  │→│ z₈₀₀  │→│ z₅₀₀  │→│ z₂₀₀  │→│ z₀(输出)│
│纯噪声   │  │粗糙轮廓│  │物体浮现│  │细节丰富│  │最终图像│
└────────┘  └────────┘  └────────┘  └────────┘  └────────┘
```

#### 5.D 3D 渲染可视化（NeRF / 3DGS）

- **NeRF**：展示从相机射出的采样射线 → 沿途采样点 → MLP 预测颜色和密度 → 体渲染积分
- **3DGS**：展示初始稀疏点云 → 高斯球优化（位置/协方差/颜色）→ 自适应致密化 → 实时渲染
- **共同特性**：提供 360° 旋转查看器，用户可拖拽旋转视角，对比 Ground Truth 与渲染结果

#### 5.E 对比学习可视化（SimCLR / CLIP）

```
┌──────────────────────────────────────────────────────────┐
│  SimCLR 对比学习流程                                      │
│                                                           │
│  [原始图像]                                               │
│      │                                                    │
│      ├── 增强1(裁剪+翻转+颜色抖动) → [正样本对 v₁] ─┐    │
│      │                                              │    │
│      └── 增强2(裁剪+灰度+模糊)   → [正样本对 v₂] ─┤    │
│                                                     │    │
│  [其他图像] → 增强 → [v₃] [v₄] ... [vₙ] ← 负样本 ─┘    │
│                                                     │    │
│  ┌──────────────────────────────────────────────────┐    │
│  │ 对比损失：拉近 v₁↔v₂，推远 v₁↔v₃, v₁↔v₄ ...     │    │
│  │                                                 │    │
│  │ 可视化：t-SNE/UMAP 降维投影，展示训练过程中       │    │
│  │ 同类图像表征逐步聚拢的过程（动图）                │    │
│  └──────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────┘
```

#### 5.F 特征演化动图（自监督学习的核心可视化）

对于 DINO / MAE / BYOL 等自监督方法，最重要的可视化是**训练过程中表征空间的演化**：

- 用 UMAP 将高维特征降到 2D，做成时间轴动图
- 用户拖动时间轴滑块，看到同类图像的聚类从散乱到聚拢的过程
- 这与只看最终结果完全不同——它展示了「学习」本身

---

## 七、模块数据模型

### 后端：AlgorithmModule（Python 类）

每个算法模块是一个继承 `AlgorithmModule` 的 Python 类，通过 `__init_subclass__` 自动注册：

```python
class AlgorithmModule:
    module_id: str = ''          # 唯一标识, 如 'edge'
    name: str = ''               # 显示名称
    name_en: str = ''            # 英文名
    phase: str = ''              # 所属阶段: phase1_fundamentals / phase2_classical / ...
    difficulty: int = 1          # 难度 1-5
    description: str = ''        # 一句话描述
    dependencies: list = None    # 前置模块 id 列表

    @staticmethod
    def get_page():
        """返回 static/pages/ 下的 HTML 文件名"""
        ...

    @staticmethod
    def get_api_endpoints():
        """返回该模块的 API 端点列表"""
        ...
```

### 模块结构约定

每个模块目录遵循统一的文件结构：

```
phase{N}_{name}/
└── {module_id}/
    ├── __init__.py      # AlgorithmModule 子类定义（注册入口）
    ├── algorithm.py     # 纯算法实现（NumPy 数学公式）
    └── processor.py     # 流水线构建器（组装步骤 + 可视化辅助）
```

### 前端：/api/modules 响应结构

```json
{
  "phases": [
    {
      "phase_id": "phase1_fundamentals",
      "phase_name": "基础原语",
      "phase_name_en": "Fundamentals",
      "emoji": "1",
      "color": "#3B82F6",
      "order": 1,
      "modules": [
        {
          "id": "grayscale",
          "name": "灰度转换",
          "name_en": "Grayscale Conversion",
          "difficulty": 1,
          "description": "...",
          "dependencies": [],
          "page": "grayscale.html"
        }
      ]
    }
  ],
  "total": 12
}
```

---

## 八、导航方式（四种发现路径）

当前已实现三种，一种规划中：

### 1. 阶段卡片网格（已实现）
```
┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│  阶段一       │ │  阶段二       │ │  阶段三       │ │  阶段四       │
│  基础原语     │ │  经典CV核心   │ │  中级视觉     │ │  深度学习     │
│  3 modules   │ │  3 modules   │ │  2 modules   │ │  4 modules   │
│  [灰度][卷积] │ │ [边缘][角点]  │ │ [匹配][频域]  │ │ [LeNet]...   │
└──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘
```
每张卡片带有阶段色标，点击模块芯片直接进入，点击卡片跳转侧边栏。

### 2. 侧边栏阶段导航（已实现）
左侧固定侧边栏按阶段分组，带色点指示器，当前模块高亮。

### 3. 难度阶梯（已实现）
```
★ (入门) → ★★ (基础) → ★★★ (进阶) → ★★★★ (高级) → ★★★★★ (前沿)
```
点击难度级别过滤侧边栏中的模块。

### 4. 全局搜索（已实现）
输入关键词搜索模块名称（中文/英文）和描述。

### 5. 依赖关系图（规划中）
可缩放的有向无环图，展示算法之间的前置依赖关系。

---

## 九、计算架构决策

### 当前策略：纯 NumPy 服务端计算

所有算法在后端用纯 NumPy 执行，中间结果通过 JSON/Base64 图像返回前端渲染。这是为了满足作业要求（禁止调用 OpenCV 等高层库）。

```
用户上传图片 → Flask API 接收
             → 保存到 static/uploads/
             → processor.build_pipeline() 执行完整流水线
             → 每一步中间结果转为 uint8 图像
             → 返回 JSON: { steps: [{id, name, image_base64, explanation}] }
             → 前端 Canvas/SVG 渲染
```

### 计算位置分布（当前实现）

| 阶段 | 已实现模块 | 计算位置 |
|---|---|---|
| 阶段一 | grayscale, convolution | 服务端 NumPy |
| 阶段二 | edge, corner, sift | 服务端 NumPy |
| 阶段三 | match | 服务端 NumPy |
| 阶段四 | lenet | 服务端 NumPy |

### 预计算策略（规划中）

- **精选示例图像**：预计算所有中间状态并缓存，即时返回
- **用户自有图像**：显示预计等待时间，提交任务
- **智能预热**：用户浏览阶段列表时，后台预加载热门算法结果

---

## 十、技术栈

### 前端

| 层面 | 技术选型 | 理由 |
|---|---|---|
| 框架 | **无框架** — 原生 HTML/CSS/JS | 作业约束：禁止引入 React/Vue 等外部 UI 库 |
| 语言 | **Vanilla JavaScript** (ES6+) | 纯函数式设计 |
| 样式 | **原生 CSS** (CSS Variables + Grid + Flexbox) | 无预处理器依赖 |
| 页面架构 | **SPA Shell + iframe** | 主外壳 hash 路由，子页面独立加载 |
| 可视化 | **Canvas 2D API** | 像素级图像/热力图渲染 |
| 矢量图形 | **SVG** | 连线、高亮框、方向箭头等低频交互元素 |
| 图像 I/O | **FileReader API + fetch + FormData** | 上传 + 结果展示 |

### 后端

| 层面 | 技术选型 | 理由 |
|---|---|---|
| 框架 | **Flask** + Flask-CORS | 轻量、同步模型契合 NumPy 计算 |
| CV 计算 | **NumPy** (纯手写) | 作业要求：禁止调用 OpenCV/skimage |
| 图像 I/O | **imageio** (读取), **Pillow** (Base64 编码) | 多格式支持 |
| 模块系统 | **importlib + \_\_init_subclass\_\_** | 零配置自动发现和注册 |

### 明确禁止

| 禁止项 | 说明 |
|---|---|
| React / Vue / Angular | 前端框架 |
| ECharts / Chart.js / D3.js | 图表库 |
| OpenCV (cv2) | 高层 CV 库（算法调用） |
| scikit-image | 高层 CV 库 |
| PyTorch / TensorFlow | 深度学习框架（阶段四之前） |

---

## 十一、当前实现状态

### 已完成（12 个模块骨架 + 7 个算法实现）

| 阶段 | 模块 | 骨架 | 算法 | 前端页面 |
|---|---|---|---|---|
| **阶段一: 基础原语** | | | | |
| | grayscale (灰度转换) | ✅ | ✅ 6种方案 | ⬜ |
| | convolution (基础卷积) | ✅ | ✅ 带trace | ⬜ |
| | live (实时滤镜) | ✅ | ⬜ | ⬜ |
| **阶段二: 经典CV** | | | | |
| | edge (边缘检测) | ✅ | ✅ Sobel+Canny | ⬜ |
| | corner (角点检测) | ✅ | ✅ Harris | ⬜ |
| | sift (SIFT特征) | ✅ | ✅ 完整流水线 | ⬜ |
| **阶段三: 中级视觉** | | | | |
| | match (特征匹配) | ✅ | ✅ 匹配+拼接 | ⬜ |
| | frequency (频域分析) | ✅ | ⬜ | ⬜ |
| **阶段四: 深度学习** | | | | |
| | lenet (LeNet-5) | ✅ | ✅ 推理+trace | ⬜ |
| | detection (目标检测) | ✅ | ⬜ [新增] | ⬜ |
| | semantic (语义分割) | ✅ | ⬜ [新增] | ⬜ |
| | instance (实例分割) | ✅ | ⬜ [新增] | ⬜ |

### 已验证的核心能力
- ✅ Flask 应用工厂 + 自动模块发现
- ✅ Hash 路由 + 阶段画廊首页 + 侧边栏导航
- ✅ 难度阶梯筛选 + 全局搜索
- ✅ iframe 子页面加载 + postMessage 通信
- ✅ 7 个算法的纯 NumPy 实现（可独立导入和测试）
- ✅ 每个算法有对应的 processor 流水线构建器

### 待建设
- ⬜ 所有模块的算法详情 HTML 页面
- ⬜ 3 个新增模块（detection/semantic/instance）的算法实现
- ⬜ 对比模式页面
- ⬜ 交互式游乐场
- ⬜ 预计算缓存机制

---

## 十二、快速提升可理解性的设计细节

| 特性 | 说明 |
|---|---|
| **卷积动画** | 卷积核在图像上滑动时做动画展示，不只是展示结果 |
| **前后对比滑块** | 每个阶段边界都有可拖拽的 before/after 对比 |
| **术语悬浮提示** | 「核」「梯度」「非极大值抑制」等术语，悬停显示通俗解释 + 小图示 |
| **「如果跳过这步？」** | 每阶段旁有一个小按钮，点击展示跳过该步的退化输出——教会用户这一步**为什么**存在 |
| **色彩约定** | 输入 = 蓝色边框，中间处理 = 灰色边框，输出 = 绿色边框。全平台统一 |
| **分享链接** | URL 包含完整参数状态，可直接分享「这个参数配置下的 Canny 效果」 |
| **中英文双语** | 界面中文，但保留英文术语标注，方便用户阅读英文论文 |

---

## 十三、项目目录结构

```
cv_comprehensive/
├── run.py                         # Flask 启动入口
├── config.py                      # 全局配置
├── requirements.txt               # flask, flask-cors, numpy, imageio, Pillow
│
├── app/                           # 后端
│   ├── __init__.py                # Flask 应用工厂 + CORS
│   ├── routes.py                  # 路由中枢: / 主页 + /api/modules + 模块API注册
│   ├── modules/
│   │   ├── __init__.py            # 模块注册表 + importlib 自动扫描 + get_modules_by_phase()
│   │   ├── base.py                # AlgorithmModule 基类 (__init_subclass__ 自动注册)
│   │   ├── phase1_fundamentals/   # 阶段一: 基础原语
│   │   │   ├── grayscale/         #   灰度转换 (algorithm + processor)
│   │   │   ├── convolution/       #   基础卷积 (algorithm)
│   │   │   └── live/              #   实时滤镜 (空壳)
│   │   ├── phase2_classical/      # 阶段二: 经典CV核心算法
│   │   │   ├── edge/              #   边缘检测 (algorithm + processor)
│   │   │   ├── corner/            #   Harris角点 (algorithm + processor)
│   │   │   └── sift/              #   SIFT特征 (algorithm + processor)
│   │   ├── phase3_intermediate/   # 阶段三: 中级视觉
│   │   │   ├── match/             #   特征匹配+拼接 (algorithm)
│   │   │   └── frequency/         #   频域分析 (空壳)
│   │   ├── phase4_deep_learning/  # 阶段四: 深度学习时代
│   │   │   ├── lenet/             #   LeNet-5 (algorithm + processor)
│   │   │   ├── detection/         #   目标检测 (空壳) [新增]
│   │   │   ├── semantic/          #   语义分割 (空壳) [新增]
│   │   │   └── instance/          #   实例分割 (空壳) [新增]
│   │   └── phase5_frontier/       # 阶段五: 前沿论文算法 (预留)
│   └── utils/
│       └── image_utils.py         # 图像工具: load/save/base64/check_shape
│
├── static/                        # 前端静态资源
│   ├── css/
│   │   └── main.css               # 主样式表 (CSS Variables + 响应式)
│   ├── js/
│   │   ├── app.js                 # 主应用: 导航渲染 + 画廊 + 详情 + iframe通信
│   │   ├── router.js              # 轻量 Hash 路由器
│   │   └── utils.js               # 纯函数工具: assertType, debounce, toast, ...
│   └── pages/                     # 各算法模块的 HTML 页面 (iframe 加载)
│       ├── grayscale.html         # ⬜ 待实现
│       ├── edge.html              # ⬜ 待实现
│       ├── ...                    # 共13个页面壳
│       ├── detection.html         # [新增] ⬜
│       ├── semantic.html          # [新增] ⬜
│       └── instance.html          # [新增] ⬜
│
├── templates/
│   └── index.html                 # Jinja2 SPA 外壳 (侧边栏 + 画廊 + iframe容器)
│
└── docs/
    ├── ARCHITECTURE.md            # 本文件
    └── ASSIGNMENT_REQUIREMENTS.md # 作业要求
```

---

## 十四、下一步行动

1. **算法详情页面** — 为每个已实现算法的模块编写 HTML 详情页（流水线条带、参数滑块、对比视图）
2. **新增三大模块** — 实现 detection（目标检测）、semantic（语义分割）、instance（实例分割）的算法核心
3. **对比模式** — 同任务不同算法的并列对比页面
4. **示例图像库** — 为每个算法精选 3-5 张典型输入图像并预计算结果
5. **安装说明文档** — 编写 README.md 含环境要求、依赖安装、启动步骤
6. **演示视频** — 录制从零到懂的通识教育完整流程
7. **保证书** — 声明核心算法为手写实现

### 当前优先级

| 优先级 | 任务 | 依赖 |
|---|---|---|
| P0 | 算法详情页面 (Pipeline Strip HTML) | 算法实现已完成 |
| P0 | 新增三大模块算法实现 | 无 |
| P1 | 示例图像 + 预计算 | 算法实现已完成 |
| P1 | 安装说明 + 保证书 | 无 |
| P2 | 对比模式 | 页面模板 |
| P2 | 演示视频 | 所有页面完成 |
| P3 | 交互式游乐场 | 对比模式 |
